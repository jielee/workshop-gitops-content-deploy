---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: day2-as-sno-10
  namespace: openshift-gitops
spec:
  generators:
  - git:
      repoURL: https://github.com/jielee/workshop-gitops-content-deploy.git
      revision: sno-10-setup
      files:
      - path: "config-definition/**/config.json" 
  template:
    metadata:
      name: 'sno-10-{{path.basename}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/jielee/helm-infra-gitops-workshop.git
        targetRevision: sno-10
        path: .
        helm:
          valueFiles:
            - '{{valuesFile}}'        
      destination:
        server: 'https://kubernetes.default.svc'
        namespace: openshift-gitops
      syncPolicy:
        automated:
          prune: true
          selfHeal: true 